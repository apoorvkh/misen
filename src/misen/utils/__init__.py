"""Internal utility modules used across misen subsystems.

These helpers support hashing, graph manipulation, settings resolution,
environment snapshots, logging capture, and other cross-cutting concerns.
"""
