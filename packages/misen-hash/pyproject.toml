[build-system]
requires = ["uv_build"]
build-backend = "uv_build"

[project]
name = "misen-hash"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "xxhash>=3.0.0",
    "dill>=0.3.6",
    "msgspec>=0.19.0",
]
