[project]
name = "misen-hash"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
  "dill>=0.4.0",  # xxh3 stable as of 2.0.0
  "msgspec>=0.20.0",
  "typing_extensions>=4.1.0",
  "xxhash>=3.0.0",
]

[build-system]
requires = ["uv_build"]
build-backend = "uv_build"
